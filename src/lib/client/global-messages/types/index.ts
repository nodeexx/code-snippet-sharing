/*
 * NOTE: Those types should be used only in `*.svelte` files, because those do
 * not support `global` types (e.g. from `app.d.ts`).
 */
export type GlobalMessage = App.GlobalMessage;
export type GlobalMessageType = App.GlobalMessageType;
