import type { Session, SessionSchema, User, UserSchema } from 'lucia';

export type AuthSession = Session;
export type AuthSessionSchema = SessionSchema;

export type AuthUser = User;
export type AuthUserSchema = UserSchema;
